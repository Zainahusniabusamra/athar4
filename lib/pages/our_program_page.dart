import 'package:flutter/material.dart';

class ourProgramPage extends StatelessWidget {
  const ourProgramPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
     child: Text('our program Page'),
    );
  }
}

